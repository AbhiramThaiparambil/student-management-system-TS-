"use strict";
class Assignment {
    constructor(title, dueDate, grade) {
        this.title = title;
        this.dueDate = dueDate;
        this.grade = grade;
    }
    setGrade(grade) {
        this.grade = grade;
    }
}
